# CLAUDE.md

Guidance for Claude Code working in this repository. Keep this file updated as the codebase changes.

## What this is
Receptionaly AI — an AI receptionist for clinics that answers patient messages 24/7, books into a real calendar, and reduces no-shows. Channel-agnostic core; **WhatsApp is the only active channel** (SMS/Twilio deferred). Currently a **demo-first** build targeting one US clinic: synthetic patients, no real PHI, consumer Anthropic key acceptable.

## Stack
Django 5 + DRF · Celery + Redis · PostgreSQL 16 · React 18 + Vite + Tailwind · Docker Compose.

## Non-negotiable principles
- **The LLM never owns the calendar.** All scheduling (availability, booking, conflict detection) is deterministic Python in `backend/apps/scheduling/engine.py`. The LLM only converses and calls validated tools. Booking uses a Postgres advisory lock + a live re-check so double-booking is structurally impossible. Slot tokens are opaque base64 the LLM echoes back; the engine re-validates them against live availability (anti-invention guarantee).
- **Channel abstraction.** Core logic sits behind a channel interface; every feature must work over plain text. Only the WhatsApp adapter is wired.
- **`clinic_id` on every table** from day 1 (single-tenant now, multi-tenant-ready for Phase 4).
- **Assistive, never clinical.** No medical advice. An emergency-keyword regex fast-path (`backend/apps/conversations/emergency.py`) bypasses the LLM entirely and escalates.

## Layout
```
backend/    Django project
  apps/clinics/        Clinic + Patient models, TCPA consent fields
  apps/scheduling/     Practitioner/Service/ScheduleRule/Appointment + engine.py (deterministic core)
  apps/conversations/  Conversation/FAQ/EscalationTicket, LLM engine.py, tools.py, prompt.py, emergency.py, inbound.py
  apps/messaging/      Message model, WhatsApp channel adapter, Celery tasks (process_inbound)
  apps/api/            DRF serializers/views/urls for the staff dashboard + seed_demo command
  config/              settings.py, urls.py
frontend/   React dashboard (Vite :5173, proxies /api to Django :8000)
infra/      docker-compose.yml, nginx
prompts/    Versioned LLM system prompts (booking_system_v1.md)
docs/       runbook.md
tests/e2e/  Conversation simulations
```

## Common commands
Compose file lives in `infra/`, so either `cd infra` first or pass `-f infra/docker-compose.yml`.
```bash
# from infra/
docker compose up --build                       # start web/worker/beat/postgres/redis
docker compose exec web python manage.py migrate
docker compose exec web python manage.py test   # backend suite
docker compose exec web python manage.py seed_demo   # seed demo clinic + staff user

# frontend (from frontend/)
npm install
npm run dev      # Vite dev server on :5173
npm run build    # production build
```

## Dev chat sandbox
Until the WhatsApp number is live, test the conversation flow via the **"Chat (test)"** dashboard tab. It posts to `POST /api/dev/chat`, which reuses the real inbound pipeline (patient upsert, consent/STOP, LLM tool loop) but skips the outbound channel send. The endpoint is **DEBUG-gated** (404 in prod) and uses a fixed demo phone `+15550000000`. `GET` returns history, `DELETE` resets (deletes the demo patient + their data). **Every POST is a live Anthropic call** — the UI disables Send while awaiting and never polls; keep it that way to avoid runaway spend. The sandbox is a single shared conversation, so only one person should drive it at a time.

## Conventions & gotchas
- **Prompts must be reachable at `/prompts` inside the container.** `prompt.py` resolves the template to `/prompts/booking_system_v1.md`. The web image is built from `../backend` only, so `prompts/` is bind-mounted (`../prompts:/prompts:ro`) in web/worker/beat. If you move prompts or change the build context, update both.
- **Celery `worker`/`beat` do NOT hot-reload.** Only the `web` container (runserver) auto-reloads on code/prompt changes. After any change that affects a Celery task path (engine, tools, prompt template, inbound pipeline), run `docker compose restart worker beat` — otherwise inbound WhatsApp messages fail silently in the worker while the dev sandbox (which runs in `web`) looks fine. This bit us once: a new `{patient_context}` prompt placeholder crashed the worker with `KeyError` until it was restarted.
- **`anthropic` must be baked into the image** (it's in `requirements.txt`). Don't rely on `pip install` inside a running container — `docker compose up` recreates from the image and loses it. Rebuild (`docker compose build web worker beat`) after dependency changes.
- **Migrations** land on the host via the `../backend:/app` bind mount — generate them in the container, they appear in the tree.
- **CSRF:** the dashboard uses DRF SessionAuthentication + CSRF. `CSRF_TRUSTED_ORIGINS` (settings.py) must include the frontend origin (`http://localhost:5173` in dev; env `DJANGO_CSRF_TRUSTED_ORIGINS` overrides). DRF only enforces CSRF once a session user exists, so login POST passes but later writes fail if the origin isn't trusted.
- **Timezones:** appointments are stored in UTC (`USE_TZ=True`). Clinic-local wall-clock times must be converted using the clinic timezone, not the browser's — see `clinicWallTimeToUTC` in `frontend/src/pages/Calendar.jsx`. DST correctness is covered by tests: slot generation (`DSTBoundaryTests`) proves a 9 AM local slot lands on the right UTC instant either side of spring-forward, and quiet-hours deferral (`QuietHoursTests`) is checked across both DST transitions.
- **API routes:** `DefaultRouter(trailing_slash=False)` — no trailing slashes. `/api/me` (not `/api/auth/me`) returns the current user + clinic. Patients are created by the inbound pipeline, not the dashboard (viewset is read-only for create).
- **US formatting:** 12-hour times in patient-facing output.

## Demo credentials / data
`seed_demo` creates clinic "Bright Smiles Dental", staff login `demo` / `demo12345`, Dr. Rivera, 4 services, Mon–Fri 9–17 hours, 5 FAQs.

## Interactive CTA booking (present_options)
Patients can tap options instead of typing. The `present_options` tool lets the LLM offer a short choice set; the reply is carried as a structured `BotReply` (`conversations/reply.py`) through the channel layer. WhatsApp renders ≤3 options as reply buttons and 4–10 as a list (`whatsapp.py::_interactive_payload`); other channels get a numbered-text fallback. A tap comes back as the option **label**, which the existing `check_availability` + `slot_token` match-and-book flow handles — the LLM still never owns the calendar. The dev sandbox renders the same options as tappable chips (`Chat.jsx`).
- **False-confirmation guardrail (built).** The model was observed telling patients an appointment was booked/moved/cancelled without calling the tool that performs it (seen repeatedly on reschedule). `conversations/engine.py` now tracks which mutating tools actually succeeded that turn (`_record_success`) and scans the outgoing reply for confirmation phrasing (`_false_confirmation`). If the reply claims a book/reschedule/cancel that didn't succeed, the model gets one forced correction turn to actually call the tool; if it still fakes it, the turn escalates to a human and sends the safe fallback — a fake confirmation can never reach the patient. Tune the regexes in `_CONFIRM_PATTERNS` if wording drifts.

## Testing notes
- Backend: 190 tests (incl. analytics tests: bookings-by-source + bot share, no-show rate excluding cancellations, out-of-range exclusion, recovered-revenue via price_min, monthly no-show trend bucketing, containment rate, median first-response, empty-range all-zeros, `/api/analytics` endpoint auth + payload, idempotent monthly-report generation, report list endpoint; waitlist tests: cancellation offers to oldest matching entries in order with a fanout cap, non-matching/opted-out/self entries skipped, idempotent per freed slot, first-confirm-wins race (loser gets "filled" + returns to active), double-tap re-confirms without double-booking, hold expiry back to active, expired/foreign-patient taps don't book, quiet-hours defer, near-term freed slot not offered, `join_waitlist` tool creates/updates one entry; no-show recovery tests: recovery pair queued on no-show (idempotent on re-save), completed/cancelled don't queue, recovery rows survive later reconciles, quiet-hours clamp, opt-out exclusion at queue and dispatch time, deterministic `recovered_from` attribution incl. negative cases, recovery template specs; reminder tests: reconcile idempotency, quiet-hours deferral, cancel-skips, dispatch idempotency/failure-retry; WhatsApp template tests: per-kind `build_template` params, 24h button payloads, `type: template` Graph payload shape, `type: "button"` inbound tap parse, text fallback when no template/creds; owner-digest template spec; and delivery-status tests: `parse_statuses` extraction, monotonic status advance, out-of-order no-downgrade, failed error capture, webhook dispatch). Live-Anthropic conversation tests are `skipUnless(ANTHROPIC_API_KEY)` — including `test_full_reschedule_flow_moves_appointment` / `test_full_cancel_flow_cancels_appointment` / `test_full_noshow_rebook_flow_attributes_recovery`, plus `test_no_availability_leads_to_waitlist_enrollment` (asserts `join_waitlist` was actually called), which assert the DB actually changed (guarding against the model faking a confirmation without calling the tool). The live tests drive turns via `_drive` (persists both sides) and tap the bot's interactive options when offered — free-text-only, unpersisted driving was a recurring flake source; multi-turn action assertions use `_drive_until_booked` / `_drive_until` (tap an offered option or nudge, looping until the DB reaches the target state) and assert the structural outcome rather than an exact count — this replaced brittle fixed-turn free-text driving that flaked ~1 in 3 full runs on the cancel/reschedule confirm dance. `ANTHROPIC_API_KEY` is set in `.env` (model `claude-haiku-4-5` — cheap), so these run. Live conversation flow is verified working via the dev chat sandbox and real WhatsApp.

## Phase status
- Phase 0: WhatsApp webhook walking skeleton — committed.
- Phase 1: booking MVP + minimal staff dashboard — built and verified end-to-end (login → calendar → manual booking).
- WhatsApp live integration + interactive CTA booking — built, verified end-to-end, pushed (`f577770`).
- Phase 2 (in progress):
  - Reschedule/cancel over chat — built and verified end-to-end (real inbound pipeline, multi-turn). `reschedule_slot` + `cancel_appointment` in `scheduling/engine.py`; `reschedule_appointment`/`cancel_appointment` LLM tools in `conversations/tools.py`; prompt section added. Reschedule is atomic (new slot committed before old is freed, in one transaction; availability excludes the appointment being moved). Ships with the false-confirmation guardrail (see the "Interactive CTA booking" section) that fixed the model faking reschedule confirmations.
  - Reminders / business-initiated messaging (outbox) — built, backend-tested (not yet exercised over live WhatsApp). See "Reminders architecture" below.
  - Appointment lifecycle — `mark_no_show` / `mark_completed` engine fns (no-show bumps `Patient.no_show_count`), exposed as `POST /api/appointments/{id}/no_show|complete`. Patient confirmation (`patient_confirmed_at`) recorded on Confirm tap / `C` reply; at-risk flag (`AppointmentSerializer.at_risk`) surfaces upcoming appts whose 24h reminder went out but the patient never confirmed. Post-appointment auto-complete + thank-you: `finalize_past_appointments` beat task (hourly) completes appts whose whole clinic-local day has ended and queues a `thank_you` outbox row (survives reconcile; gated on `reminders_enabled`).
  - Dashboard schedule management — Today view with day summary + at-risk count, Complete/No-show/Cancel lifecycle buttons, and a month-to-date messaging cost pill (`frontend/src/pages/Calendar.jsx`).
  - **WhatsApp message templates wired.** The 4 patient-facing templates are approved in Meta (`appointment_confirmation`, `appointment_reminder_24h` — with Confirm/Reschedule/Cancel quick-reply buttons — `appointment_reminder_2h`, `appointment_thank_you`, all `en_US`). `reminders.build_template` produces the per-kind template spec (ordered body params + button payloads); `whatsapp.py::send_template` builds the real `type: template` Graph payload, and a tapped template button arrives as `type: "button"` with the routing id in `payload` (parsed in `_parse_message`). Plain text remains the fallback when no template maps or credentials are missing. **Not yet exercised over a live send** — verify the first real template send end-to-end.
- Next (per revised plan): finish Phase 2 hardening → Phase 3 → Phase 4 (superuser-operated multi-tenant admin: superuser adds/deletes clinics, manages subscriptions + pay status manually, payment handled off-platform — no self-onboarding, no payment processor). **Production deployment is deferred until after Phase 4**, followed by a complete professional UI revamp. CI runs build+test from now, but prod deploy/infra is not stood up per-phase. Authoritative plan: `~/Downloads/ai-receptionist-build-plan.md`.
  - **Owner digest template wired.** `digest.py::build_owner_digest_template` produces a fixed 5-param spec (`owner_daily_digest`, `en_US`: clinic, date, count, first arrival, status note — every case folded into params since templates can't do conditionals); `build_owner_digest` stays the plain-text fallback. `send_owner_digests` now sends via `send_template`. **The `owner_daily_digest` template must be created + approved in Meta before live sends.**
  - **Delivery-status reconcile wired.** Meta status callbacks (`value.statuses`) are parsed by `whatsapp.py::parse_statuses` → `StatusUpdate`, enqueued from the webhook, and applied by `tasks.py::process_status`, which advances `Message.delivery_status` (sent→delivered→read, monotonic so out-of-order receipts don't downgrade; `failed` always wins and records `delivery_error`). Failed sends surface on the dashboard via `failed_deliveries` on `GET /api/costs` + a red pill in `Calendar.jsx`.
- Phase 3 (in progress) — revenue intelligence. Order: no-show recovery → waitlist → analytics → recalls → multi-practitioner → quality loop. Waitlist slot-open offers are **utility** category (patient requested them); voice notes deferred past Phase 3.
  - **Slice 5: analytics + monthly owner report — built, backend-tested + verified live.** `apps/api/analytics.py` holds pure, tz-aware aggregation (bookings by `Appointment.source` + bot share, no-show rate + monthly trend over decided visits, recovered revenue = `recovered_from` bookings × `price_min`, waitlist fills = accepted `WaitlistOffer`s, containment = 1 − escalated/active conversations, median first-response from `Message` timestamps) over a `DateRange`; date bucketing is clinic-local. `GET /api/analytics?from=&to=` (default current clinic-local month) and `GET /api/reports/monthly` (stored snapshots). `clinics.MonthlyReport` (UNIQUE(clinic, year, month)) is frozen once per month by `messaging.tasks.generate_monthly_reports` (beat, daily; get_or_creates the *previous* clinic-local month so it's idempotent). Frontend: an **Analytics** tab (`frontend/src/pages/Analytics.jsx`) with the headline recovered-revenue number, stat cards, a no-show trend, bookings-by-source bars, and a monthly-report viewer. No new Meta template (report is dashboard-only; owner WhatsApp summary can be added later via the digest path).
  - **Slice 2: waitlist — built, backend-tested** (branch `feature/waitlist`; `waitlist_slot_open` template pending Meta approval). See "Waitlist architecture" below. Marquee demo: cancel an appointment → a waitlisted patient's phone buzzes with a tappable "spot just opened" offer → they tap → calendar updates. **First-confirm-wins** is enforced by the existing `book_slot` advisory lock + live re-check (never a new lock); the loser gets a graceful "just filled" and returns to the waitlist. New template to submit in Meta: `waitlist_slot_open` (utility — the patient requested the notification).
  - **Slice 1: no-show recovery — built, backend-tested** (branch `feature/no-show-recovery`; templates pending Meta approval). Marking a no-show queues a two-step recovery sequence through the existing outbox: `recovery_sameday` (due now) + `recovery_rebook` (due +2 days), both quiet-hours-clamped, gated by `clinic.reminders_enabled` **and** `clinic.no_show_recovery_enabled` (default True), and never queued for opted-out patients. These are post-appointment kinds — reconcile-skip only touches `_PRE_APPOINTMENT_KINDS`, so they survive. The rebook template bakes up to 3 live openings into a body param (computed at dispatch, `reminders._rebook_openings_sentence`) and carries one quick-reply button whose payload `rebook_appt_{id}` routes back through `parse_option_id` → `inbound._reminder_action` (matches the **no-show** appt, unlike other actions which need active ones) → synthesized booking request incl. the service id (the model was observed guessing `service_id` otherwise) → normal `check_availability` + `slot_token` flow. **Attribution is deterministic, never the LLM:** `Appointment.recovered_from` (self-FK) is set by `reminders.attribute_recovered_booking` on the post_save create signal when the patient had a recovery message SENT within the last 14 days — recovered bookings/revenue for the Slice 5 dashboard. New templates to submit in Meta: `noshow_recovery_sameday`, `noshow_rebook_offer`. Also added: the dispatcher now skips (never sends) any outbox row whose patient opted out after queueing.

## Reminders architecture (outbox + beat dispatch)
- **Outbox model** `messaging.ScheduledMessage`: one row per (appointment, kind) — `UNIQUE(appointment, kind)` makes idempotency structural (a reminder can exist at most once, so it can never double-send). Kinds: `confirmation` (due now), `reminder_24h`, `reminder_2h`, `thank_you` (post-visit). The first three are *pre-appointment* (`reminders._PRE_APPOINTMENT_KINDS`) and are the only ones a reconcile marks `skipped` when the appointment leaves an active state — so completing an appointment never skips its own thank-you.
- **Reconciliation is signal-driven** so `scheduling` stays free of any messaging import: a `post_save` on `Appointment` (wired in `messaging/apps.py::ready`) calls `reconcile_appointment_reminders` (`messaging/reminders.py`). Active appointment → confirmation + still-future reminders exist (get_or_create); cancelled/rescheduled/completed/no_show → its unsent rows are marked `skipped`. Reminders whose send time already passed (e.g. a booking made <24h out) are skipped, never back-dated. Gated by `clinic.reminders_enabled`.
- **Dispatch** `messaging/tasks.py::dispatch_due_messages` runs on Celery beat every 5 min (`CELERY_BEAT_SCHEDULE` in settings). Claims due `pending` rows under `select_for_update(skip_locked=True)` so parallel workers never double-send and a crashed worker leaves the row `pending` for retry. Every kind sends through `channel.send_template` with the `build_template` spec (reminders are business-initiated, i.e. outside the 24h window, so a Meta-approved template is required); the plain-text `build_body` is passed as the fallback for channels/creds without template support. Logs an outbound `Message` (`message_type="template"`). Failures keep the row `pending` (→ `failed` after 5 attempts).
- **TCPA quiet hours** (`clinic.quiet_hours_start/end`, clinic-local): a row due outside the window is deferred — its `scheduled_for` is pushed to the next window open, never dropped. Logic in `reminders.py::next_send_time` (handles same-day and overnight windows).
- **Reminder bodies are PHI-minimal** (`build_body`): date/time/clinic only, never procedure details.
- **Message cost estimate** (`messaging/costs.py` + `MessageRate` table): outbound messages snapshot a `category` (WhatsApp conversation category — reminders are `utility`, in-session replies `service`/free) and a `cost_amount` at send time, so a later rate change never rewrites history. Default per-message rates are seeded by migration `0008` (utility $0.04, marketing $0.0625, auth $0.0135, service $0). `GET /api/costs?from=&to=` (default current month) returns total + per-category breakdown for the clinic. Estimate only — real WhatsApp billing is per-conversation.
- **Auto-complete** `messaging/tasks.py::finalize_past_appointments` (beat, hourly): per active clinic, completes still-active appointments whose `ends_at` is before the clinic-local start of *today* (a full-day grace so staff can mark no-show), via `mark_completed` (which fires the reconcile signal → skips pending pre-appointment reminders), then queues a `thank_you` row when `reminders_enabled`. Idempotent via `get_or_create` on the unique (appointment, kind).
- **Owner digest** `messaging/tasks.py::send_owner_digests` (beat, hourly): once-a-day morning summary to `clinic.owner_phone_e164` (blank disables it). Sends when clinic-local hour is in `[owner_digest_hour, 12)` — the window gives a failed 8am send room to retry the same morning. Idempotent via `messaging.OwnerDigest` UNIQUE(clinic, date) (get_or_create claims the day; a send failure deletes the claim so a later run retries). Sends the approved `owner_daily_digest` template (`build_owner_digest_template`, fixed 5-param shape) with `build_owner_digest` as the plain-text fallback — goes to the business, never a patient.

## Waitlist architecture (Phase 3 Slice 2)
- **`scheduling.Waitlist`**: one *active* entry per (patient, service) — `join_waitlist` (LLM tool) refreshes an existing entry rather than stacking duplicates that would all fire on one freed slot. Fields: optional `practitioner`, `date_from`/`date_to` window (null = open-ended), `time_preference` (`any|morning|afternoon|evening`; windows shared with slot filtering via `engine.PREFERENCE_WINDOWS`), `status` (active/offered/booked/expired/cancelled). The prompt offers it when `check_availability` returns nothing.
- **Matching is deterministic** (`engine.match_waitlist`): oldest active entries (`created_at`) whose service + practitioner-compat (a no-preference entry accepts any doctor; a specific one must match the freed slot's doctor) + date window + time-of-day preference fit the freed slot. Excludes opted-out patients and the patient who freed the slot.
- **Fill on cancellation is signal-driven** (same `Appointment` post_save as reminders, plus a `pre_save` that stashes `_old_status` so post_save can detect the *transition into* cancelled/rescheduled — a re-save doesn't re-fire). On a real transition with a future start, `transaction.on_commit` enqueues `offer_waitlist_slot` **after commit** (so the worker's live re-check sees the slot as free). That task calls `waitlist.create_offers` (guards `freed_slot_available`: future, past min-notice, still open) then sends immediately — beat's `process_waitlist_offers` (5 min) only handles quiet-hours deferrals, retries, and hold expiry.
- **Offer outbox** `messaging.WaitlistOffer`: `UNIQUE(waitlist, freed_appointment)` makes re-processing a freed slot structurally unable to double-offer. Fanout `OFFER_FANOUT=3` per slot; each offer holds `OFFER_HOLD=2h` (or until the slot start, whichever is sooner). Same claiming discipline as the reminder dispatcher (`select_for_update(skip_locked=True)`, quiet-hours defer, pending-on-failure retry → expired after 5 attempts). Utility category (patient requested the notification — decision 2026-07-07); opt-out honored at queue and send time.
- **First-confirm-wins** (`waitlist.accept_offer`, routed deterministically from a tapped `waitlist_offer_{id}` in `inbound.handle_inbound` — **no LLM**, so it works even if the model is down): delegates to `engine.book_slot`, whose advisory lock + live re-check means only the first tapper books. The loser's entry returns to `active` and they get a graceful "just filled" reply. Double-tap re-confirms (idempotent). Runs even when a human owns the conversation — the hold is short and silence would cost the patient the slot.
- **Hold expiry** `waitlist.expire_stale_offers` (in the beat task): sent offers past `offer_expires_at` and pending offers whose slot start passed go `expired`, and their entry returns to `active` (or `expired` if its desired window is now in the past) so the next freed slot can be re-offered.
- **Template**: `waitlist_slot_open` (`build_offer_template`: first name, clinic, slot time + one "Grab it" quick-reply button carrying `waitlist_offer_{id}`), `build_offer_body` plain-text fallback. **Must be created + approved in Meta before live sends.**
